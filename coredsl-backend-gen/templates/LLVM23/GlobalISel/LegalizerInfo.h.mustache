#ifndef LLVM_LIB_TARGET_{{target_upper}}_LEGALIZERINFO_H
#define LLVM_LIB_TARGET_{{target_upper}}_LEGALIZERINFO_H

#include "llvm/CodeGen/GlobalISel/LegalizerInfo.h"

namespace llvm {

class {{target}}LegalizerInfo final : public LegalizerInfo {
public:
  {{target}}LegalizerInfo();

  bool legalizeIntrinsic(LegalizerHelper &Helper,
                         MachineInstr &MI) const override;
};

} // namespace llvm

#endif // LLVM_LIB_TARGET_{{target_upper}}_LEGALIZERINFO_H
