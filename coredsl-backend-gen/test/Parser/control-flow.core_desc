InstructionSet Control {
  instructions {
    ACCUMULATE {
      behavior {
        let i = 0;
        while (i < 4) {
          X[rd] += X[rs1];
          i++;
        }
        for (let j = 0; j < 2; j += 1) {
          X[rd] = X[rd] ^ X[rs2];
        }
      }
    }
  }
}
